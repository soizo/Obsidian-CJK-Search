# CJK Search 需求与方案

当前阶段：**0.3.0 已安装到批准测试库**。原生英文设置提供 Search／Find／Quick switcher 独立开关和 Compatibility characters 开关。10 个 Python 与 42 个 Node 测试、macOS / Obsidian 1.13.7 设置 UI 和两套真实搜索回归通过；5 个产物一致，13 个非产物文件不变。见 [0.3.0 验证报告](docs/research/user-settings.md)。仅作者自审，无子代理或 Git 操作；iOS 新入口／设置待用户复测，Graph View 留待后续。以下保留历史步骤与原有验收结论。

已确认的数据规则：
- 仅使用 Unicode 官方数据及 OpenCC 官方字符字典，不引入地区词汇转换。
- 优先召回，`发／發／髮` 对称互搜。
- 用户已确认日文多义合并默认参与跨地区互搜，例如 `弁／瓣／辨／辯`；不为这类关系另外设置默认关闭的开关。
- 上述批准不扩大到形似混淆、专门语义异体或未经筛选的一般语义关系。

- [x] 检查项目现状：目录无产品代码、项目文档或 Git 仓库。
- [x] 澄清并批准本轮产品行为、数据语义及默认／全量边界。
- [x] 调查异体与编码兼容数据的来源、语义、许可：见 `docs/research/variant-data-sources.md`。
- [x] 完成已启动的 Unicode 17／18 数据包条目核验并补充证据：见 `docs/research/evidence/`；仅数据核验，不代表完整覆盖或插件集成验证。
- [x] 小字表桌面探针通过（macOS / Obsidian 1.13.7）：原生预期失败基线、九组程序调用查询、实际输入、原输入及历史、匹配偏移、原生高亮与点击、状态恢复、禁用后下一次查询恢复、笔记不变。证据见 `docs/research/native-search-spike.md`；不代表正式功能或完整兼容验证。
- [x] iOS / Obsidian 1.13.7、插件 0.1.0 用户手动验收通过。用户纠正：此前测试的是当页 Find／文件名搜索，而非全库搜索；在真正全库搜索中汉字增强及全量开关均符合预期，关闭全量后兼容字符不再额外命中。撤回此前“iOS 全库接入异常”的判断；旧日志仅描述当时获取的视图，不作为真实搜索入口失败的证据。Android 仍未验证，不重启模拟器／5038 服务。
- [x] 验证本轮原生搜索集成、约定数据覆盖和跨表连通分组；不宣称覆盖所有异体或未来版本兼容。
- 视觉辅助：本轮不适用，使用 Obsidian 原生单开关设置。
- [x] 比较构建期离线字表与运行时原始数据／OpenCC 运行时，选择构建期生成。
- [x] 确认本轮接入范围，日文多义合并默认开启；用户回复“开始”。
- [x] 书面设计：`docs/superpowers/specs/2026-09-21-full-character-data-design.md`，未自动提交。
- [x] 自审设计的完整性、一致性、范围及歧义；明确数据筛选、序列／非 BMP、回退上限和测试库更新边界。
- [x] 书面设计已获用户批准（回复“可以”）。
- [x] 实施计划及自审：`docs/superpowers/plans/2026-09-21-full-character-data.md`。
- [x] 用户审阅实施计划并选择主会话直接执行，不使用子代理。
- [x] Task 1：Unicode 18.0.0／OpenCC 1.4.2 官方数据锁定、导入、审计和可重复构建。
- [x] Task 2：完整字表／多字符序列的有界查询展开；197,228 次双向检查，730 个语法保留方向明确跳过。
- [x] Task 3：原生适配、兼容开关、隐私诊断、生命周期和打包。
- [x] Task 4：实际 Obsidian 完整数据回归；真实设置窗口、持久化、双面板、范围／点击及故障回退，pageErrors 为空。
- [x] Task 5：只更新测试库的 5 个插件产物；旧文件已备份，8 个非产物文件不变，文档与自审完成。
- [x] 用户确认 macOS 手动测试全部成功；iOS 全库搜索亦由用户确认正常（非本机自动化验证）。
- [x] 用户批准新范围：全量兼容默认开启但保留旧布尔设置；Find 只查找不扩展替换；Open file 增强文件名／别名，保留原生界面及模糊搜索。
- [x] 只读检查 Obsidian 1.13.7 两入口结构；完成并自审 [Find / Open file 设计](docs/superpowers/specs/2026-09-22-find-open-file-design.md)，区分编辑／阅读模式、原生既有匹配、原文偏移与替换安全。
- [x] 新书面设计获用户批准（回复“开始实施”）；沿用主会话直接执行，不派子代理。
- [x] 完成并自审 [Find / Open file 实施计划](docs/superpowers/plans/2026-09-22-find-open-file.md)：先固定原生基线，再共享匹配／偏移、Find、Open file、默认设置、桌面验收与安全安装。确认原生编辑 searchCursor 接收字符串，不能直接传入 RegExp。
- [x] 用户确认实施计划（“确认开始”），开始主会话 TDD；进度记录 `.pi/find-open-file-progress.md`。
- [x] 新入口原生基线通过，旧版 Find 按预期 RED；共享字面匹配／UTF-16 偏移核心通过完整回归。
- [x] 0.2.0 Find／Open file、默认开启与旧设置保留；单元测试 RED→GREEN，真实输入／计数／导航／替换安全／原文件打开、原生恢复及边界验收通过。阅读模式保留原生内联标题搜索。
- [x] 完成作者自审、文档与安全安装；收尾的外来阅读钩子卸载缓存问题已 RED→GREEN 并完整复验。最终替换前备份 `.pi/plugin-backups/before-install-D8tG3N/`，原 0.1.0 备份仍在 `before-install-RPsoPL/`；5 个产物与最终实测哈希一致，12 个非产物文件（包括已有设置）完全不变。
- [x] 用户反馈 0.2.0“成功，非常成功”；未明确设备，不将其自动记为 iOS 新入口验收。
- [x] 用户批准先完善面向用户的英文设置：三个入口独立开关、兼容字符开关、核心功能未启用提示、安静加载与集中英文文案；继续主会话执行。
- [x] 0.3.0 英文设置、独立开关、保存回滚及退役钩子／监听清理完成；真实 UI 和完整搜索回归通过，浅色／深色／窄窗截图已检查。安全安装后 13 个非产物不变，备份 `.pi/plugin-backups/before-install-E9MRiL/`。
- [ ] 后续实现 Graph View 搜索／过滤增强，届时再加入对应开关；本轮不实现、不显示无效开关。
- [ ] iOS 新入口及新设置仍需明确的用户真机复测。
- [ ] 后续版本与 Windows／Linux、大库性能、IME 真机输入及更多主题／无障碍场景验证。
- [ ] 开发工具静态诊断仍有未消除报告；不将测试通过表述为所有静态检查全绿，详情见验证报告。
