# 标签与双链补全（0.5.0）

状态：最终桌面回归通过，0.5.0 已安全更新到批准测试库；新补全的移动端验收仍待完成。

## 用户确认的范围

新增 **Tags**（`tagsEnabled`）与 **Internal links**（`internalLinksEnabled`），两者独立、默认 true，旧设置与无关字段保留，不在加载时写迁移。

- Markdown 编辑器中 `#研` 可补全 `#硏究/體`，选中后仍插入原标签。
- `[[体育` 可补全「體育」；文件名、原始路径与别名参与，选中由原生代码生成正确链接和别名。
- 原生 Shift+Enter 新建／按输入插入、已有候选分数与对象、空输入、未解析链接均保留。标题、块引用、显示文字模式和非顶层 Markdown 编辑器不增强。
- 复用字表、原生评分与 UTF-16 偏移映射；不重写输入、不修改候选文件或 metadataCache、不替换全局原型。
- 标签遵守原生小写评分；长度变化的大小写折叠保守回退，避免错误高亮。超限、坏接口或映射错误整次回退，不返回部分追加。
- 只包装已有原生补全实例，保留原生弹层和选择逻辑；关闭／退役后保留的包装器不重新生效，取消或迟到的异步结果不追加匹配。

## 验证

- b7c6fc729：原生标签／双链基线通过，旧 0.4.0 在标签字符等价处按预期失败。
- 匹配／偏移／限额、默认选项／设置和原生标签评分先 RED 后 GREEN；补充独立开关、异步取消、第三方包装器保留及卸载测试。
- b8548531b：首轮 10 Python＋56 Node、构建、补全／设置＋Find/Open file／Graph／全库原生回归均通过，无页面错误。
- b5ff72350：最终 10 Python＋57 Node、构建与四套原生回归退出 0；四份 pageErrors 均为空。期间纠正了测试假设：全量模式会先分解 İ 并已有正确偏移，长度变化的小写回退用关闭全量的场景验证。
- 实际输入、原标签插入、文件及别名链接插入、Shift+Enter、原生标题模式、停用后的原生恢复和夹具文件不变均检查；截图检查了标签／双链弹层与桌面／600px 设置。八项设置沿用原生纵向滚动，不要求全部挤在一屏。
- 使用隔离 macOS / Obsidian 1.13.7 实例；没有操作用户运行中的库。iOS／Android／其他桌面平台、新补全大库性能和完整 IME／多窗口矩阵未验收。
- 主会话作者自审，无子代理、分支、提交或推送。LSP 6 文件返回 0 errors，但仅 1 confirmed clean、5 inconclusive；不能声称全静态检查全绿。

## 产物与安装

- 主包 SHA-256：`99a420604db65eef96e867e86583608f1ef8a4e6ca58ecd04d444f5f3ed2da8e`。
- manifest SHA-256：`e7c1ba89156cde639468982538f8fb1f7f62354d82633bef33fc6e719be9d2ca`。
- 字表仍为锁定版本，未重新下载或更改选定关系。
- 安装前新快照；5 个白名单产物与四套实测哈希一致，14 个非产物文件（含笔记、data.json、工作区与核心配置）不变。
- 备份：`.pi/plugin-backups/before-install-Epv89G/`。

证据目录：`docs/research/evidence/editor-suggestions/`，包含四份 JSON、`verification.log`、保留的首轮日志、截图、`pre-install-hashes.json` 与 `installation.json`。该目录仅本地保留，不纳入 Git。
